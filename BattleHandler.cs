using UnityEngine;

public class BattleHandler : MonoBehaviour
{
    /*
    private manager manager;
    public GameObject[] enemyObjects = {e1, e2, e3 , e4};
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        foreach (GameObject enemy in enemyObjects)
        {
            enemyObjects[enemy].SetActive(false);
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    void BattleStart()
    {
        EnemyInitializer();
    }


    public void EnemyInitializer()
    {
        foreach (Entity enemy in manager.enemies)
        {
            InitializeEnemyModel(enemy.IndexOf());
        }
    }

    public void InitializeEnemyModel(int x)
    {
        enemyObjects[x].SetActive(true);
    }
    */
}
